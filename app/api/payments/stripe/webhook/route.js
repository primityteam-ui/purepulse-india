import {stripe} from '@/lib/stripe';import connectDB from '@/lib/mongodb';import Order from '@/models/Order'
export async function POST(request){try{const body=await request.text();const sig=request.headers.get('stripe-signature');const event=stripe.webhooks.constructEvent(body,sig,process.env.STRIPE_WEBHOOK_SECRET);if(event.type==='payment_intent.succeeded'){await connectDB();const pi=event.data.object;await Order.findOneAndUpdate({paymentIntentId:pi.id},{paymentStatus:'paid',orderStatus:'confirmed'})}return Response.json({received:true})}catch(e){return Response.json({error:e.message},{status:400})}}
